apiVersion: v1
kind: ConfigMap
metadata:
  name: javaline-config
  namespace: javaline
data:
  DATABASE_HOST: "javaline-db"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "javaline"
  OPENAI_MODEL: "gpt-4o-mini"
  CORS_ORIGINS: "http://localhost,http://localhost:80"
